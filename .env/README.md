# Environment files

Use these files in VS Code:

- `.env.example`: shared template (safe to commit)
- `.env.local`: your local secrets (do not commit)

If your Wrike account is hosted in EU, set:

`WRIKE_HOST=app-eu.wrike.com`
